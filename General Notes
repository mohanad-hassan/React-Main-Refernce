//we use e.preventDefault() because the default behavior for form is to collect the data and send them to the url you defined in action .
//onSubmit fires action when the user clics the button its type is submit .
//    npm create vite@latest my-react-app -- --template react   is faster than create react-app  for creating , developmening and production .
//in vite , we have to use jsx for file formats .
//it creates dist folder when builing , not build .
//vite is alternative for wepback .
// or npm init vite .
//their is new library called REACT QYERY , for fethcing data inasted of set useEffect and is more faster and shorter code .
// in react Devtools , Profiler shows wich compnents rerndering and why . SO SO SO IMPORTANT 
// When you see clg Twice , its because of React.StrictMode wich works only in development mode .
//

